{
  "root": "packages/iocuak-reflect-metadata-utils",
  "sourceRoot": "packages/iocuak-reflect-metadata-utils/src",
  "projectType": "library",
  "targets": {
    "build": {
      "dependsOn": ["^build"],
      "executor": "nx:run-commands",
      "inputs": ["{projectRoot}/src/**/*.ts"],
      "outputs": ["{projectRoot}/lib"],
      "options": {
        "commands": [
          "pnpm run build:clean",
          "pnpm run build"
        ],
        "cwd": "packages/iocuak-reflect-metadata-utils"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "inputs": ["{projectRoot}/src/**/*.ts"],
      "outputs": [],
      "options": {
        "lintFilePatterns": ["packages/iocuak-reflect-metadata-utils/src/**/*.ts"]
      }
    },
    "publish": {
      "dependsOn": ["build", "^publish"],
      "executor": "@cuaklabs/nx-plugin:run-commands-interactive",
      "inputs": ["{projectRoot}/**"],
      "outputs": [],
      "options": {
        "commands": [
          "pnpm exec iocuak-publish ./packages/iocuak-reflect-metadata-utils"
        ]
      }
    }
  }
}
