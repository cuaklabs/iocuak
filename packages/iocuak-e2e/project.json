{
  "root": "packages/iocuak-e2e",
  "sourceRoot": "packages/iocuak-e2e/src",
  "projectType": "library",
  "targets": {
    "build": {
      "dependsOn": ["^build"],
      "executor": "nx:run-commands",
      "inputs": ["{projectRoot}/src/**/*.ts"],
      "outputs": ["{projectRoot}/lib"],
      "options": {
        "commands": [
          "pnpm run build:clean",
          "pnpm run build"
        ],
        "cwd": "packages/iocuak-e2e"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "inputs": ["{projectRoot}/src/**/*.ts"],
      "outputs": [],
      "options": {
        "commands": [
          "pnpm run lint ./packages/iocuak-e2e"
        ],
        "cwd": "."
      }
    }
  }
}
