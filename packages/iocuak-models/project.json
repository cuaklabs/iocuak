{
  "root": "packages/iocuak-models",
  "sourceRoot": "packages/iocuak-models/src",
  "projectType": "library",
  "targets": {
    "build": {
      "dependsOn": ["^build"],
      "executor": "nx:run-commands",
      "inputs": ["{projectRoot}/src/**/*.ts"],
      "outputs": ["{projectRoot}/lib"],
      "options": {
        "commands": [
          "pnpm run build:clean",
          "pnpm run build"
        ],
        "cwd": "packages/iocuak-models"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "inputs": ["{projectRoot}/src/**/*.ts"],
      "outputs": [],
      "options": {
        "lintFilePatterns": ["packages/iocuak-models/src/**/*.ts"]
      }
    }
  }
}
