{
  "root": "packages/iocuak-decorators",
  "sourceRoot": "packages/iocuak-decorators/src",
  "projectType": "library",
  "targets": {
    "build": {
      "dependsOn": ["^build"],
      "executor": "nx:run-commands",
      "inputs": ["{projectRoot}/src/**/*.ts"],
      "outputs": ["{projectRoot}/lib"],
      "options": {
        "commands": [
          "pnpm run build:clean",
          "pnpm run build"
        ],
        "cwd": "packages/iocuak-decorators"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "inputs": ["{projectRoot}/src/**/*.ts"],
      "outputs": [],
      "options": {
        "lintFilePatterns": ["packages/iocuak-decorators/src/**/*.ts"]
      }
    },
    "publish": {
      "dependsOn": ["build", "^publish"],
      "executor": "nx:run-commands",
      "inputs": ["{projectRoot}/**"],
      "outputs": [],
      "options": {
        "commands": [
          "pnpm exec iocuak-publish ./packages/iocuak-decorators"
        ]
      }
    }
  }
}
