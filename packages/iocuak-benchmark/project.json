{
  "root": "packages/iocuak-benchmark",
  "sourceRoot": "packages/iocuak-benchmark/src",
  "projectType": "library",
  "targets": {
    "build": {
      "dependsOn": ["^build"],
      "executor": "nx:run-commands",
      "inputs": ["{projectRoot}/src/**/*.ts"],
      "outputs": ["{projectRoot}/lib"],
      "options": {
        "commands": [
          "pnpm run build:clean",
          "pnpm run build"
        ],
        "cwd": "packages/iocuak-benchmark"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "inputs": ["{projectRoot}/src/**/*.ts"],
      "outputs": [],
      "options": {
        "lintFilePatterns": ["packages/iocuak-benchmark/src/**/*.ts"]
      }
    }
  }
}
